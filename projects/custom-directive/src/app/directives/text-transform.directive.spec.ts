import { TextTransformDirective } from './text-transform.directive';

describe('TextTransformDirective', () => {
  it('should create an instance', () => {
    const directive = new TextTransformDirective();
    expect(directive).toBeTruthy();
  });
});
